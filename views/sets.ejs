<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lego Sets</title>
    <link rel="stylesheet" href="/css/main.css">
  </head>
  <body tabindex="-1">

    <%- include('partials/navbar') %>
  
    <br>

    <div class="container mx-auto p-4">
      <div class="hero bg-base-200 rounded-lg p-6">
        <div class="hero-content text-center ">
          <div class="max-w-md">
            <h1 class="text-5xl font-bold">Collection</h1>
            <p class="py-6">
                Browse <b>part</b> of the lego collection here. <br> Click the names to filter by themes such as: <br>
                <a href="/lego/sets?theme=icons" class="btn btn-primary btn-xs">Icons</a>
                <a href="/lego/sets?theme=lion knights" class="btn btn-primary btn-xs" >Lion Knights</a>
                <a href="/lego/sets?theme=books" class="btn btn-primary btn-xs" >Books</a>
                <a href="/lego/sets?theme=basic set" class="btn btn-primary btn-xs" >Basic Set</a>
                <a href="/lego/sets?theme=playhouse" class="btn btn-primary btn-xs" >Playhouse</a>
            </p>
          </div>
        </div>
      </div>

      <div class="overflow-x-auto overflow-y-hidden">
        <table class="table table-zebra">
            <% sets.forEach(set => { %>
              <tr>
                <td>
                    <div class="avatar">
                        <div class="w-32 h-32 mask mask-squircle rounded-lg bg-transparent bg-no-repeat bg-cover " style="background-image: url('<%= set.img_url %>');"></div>
                    </div>
                </td>
                <td>
                    <%= set.name %> | <a href="/lego/sets?theme=<%= set.Theme.name %>" class="btn mtn-2 btn-xs"><%= set.Theme.name %></a>
                </td>
                <td>
                    <span class="font-bold">Year: </span>
                    <%= set.year %> -
                    <span class="font-bold">Parts: </span>
                    <%= set.num_parts %>
                </td>
                <th><a href="/lego/sets/<%= set.set_num %>" class="btn btn-sm">details</a></th>
              </tr>
            <% }); %>
          </table>

      </div>

      
  </body>
</html>